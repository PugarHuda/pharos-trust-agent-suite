{
  "name": "intent-mandate",
  "version": "1.0.0",
  "description": "A cryptographic leash for agents (AP2-style Intent Mandate): a user signs an EIP-712 envelope authorizing an agent to spend up to a cap, to allowed recipients, before expiry. The agent cannot exceed it.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mandate": "./scripts/mandate.mjs"
  },
  "scripts": {
    "build": "node scripts/compile.mjs",
    "test": "node --test tests/mandate.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "solc": "^0.8.26",
    "@ethereumjs/evm": "^3.1.0",
    "@ethereumjs/common": "^4.4.0",
    "@ethereumjs/util": "^9.1.0"
  }
}
